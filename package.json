{
  "name": "elias-thorne-pwa",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Elias Thorne \u2014 an autonomous on-device agent (BitNet 2B4T / WebGPU) delivered as a PWA and packaged for Android as a Trusted Web Activity.",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20.19"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "icons": "node scripts/generate-icons.mjs",
    "twa:init": "bubblewrap init --manifest=https://elias-thorne.example/manifest.json --directory=android",
    "twa:build": "bubblewrap build --manifest=android/twa-manifest.json",
    "test": "node scripts/check.mjs"
  },
  "dependencies": {
    "@orama/orama": "^3.1.18",
    "onnxruntime-web": "^1.30.0"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
